# 研究メモの運用ルール

この文書は、教材になる前の調査結果をどこにどう溜め、
いつ・どうやって教材へ反映するかを定めます。

## 1. この文書の位置づけ

[AUTHORING.md](AUTHORING.md) が扱うのは「教材の中身が正しいか」です。
この文書が扱うのは「教材化する前の調査結果を、どこにどう溜めておくか」です。
役割が違うので、文書もチェックの仕組みも分けています。

- 教材の本文・サンプルコードの正しさ → `AUTHORING.md`
- 教材になる前の調査メモの置き場・書式・反映手順 → この文書

## 2. いつ書くか

**指示がなくても書きます。** ある分野について会話・調査をして分かったことは、
その場でメモとして残してください。

逆に、メモを教材へ**正式に反映するのは、指示があったときだけ**です。
指示なくメモを書くことと、指示なく教材本文を書き換えることは、まったく別の行為です。
後者は必ずユーザーの指示を待ちます。

**メモの内容は、ユーザーからのどういう質問・要望に対する説明なのかを必ず記載します。**
初回の内容だけでなく、後から追記する内容（→ 7 節）についても同様です。
書き方は 5 節・7 節、検証は 9 節を見てください。

## 3. どこに書くか

`<教材名>/メモ/` に Markdown ファイルとして置きます。

教材フォルダ（`book/`・`tools/`・`examples/` など）がまだ無い、
検討段階の分野でも構いません。対象の教材名が決まった時点で、
`<教材名>/メモ/` だけ先に作ってよいことにしています
（`book/toc.py` などが無くても、フォルダと `README.md` だけは存在してよい）。

ファイル名は kebab-case（例: `playground-run-feature.md`）。
メモは順序を持たないので、連番は付けません。

## 4. 粒度の分け方

- **体系立てて順序よく書くべき内容**（ある言語の基礎文法一覧など）は
  1 つのファイルにまとめます
- **1 つの疑問・1 つの文法点の深掘り**（より分かりやすい説明、後から出た疑問への回答など）は
  1 トピック 1 ファイルにします

迷ったら、分割する側に倒してください。後で統合するより、後で結合する方が楽です。

## 5. 新規メモファイルの雛形

```markdown
# <トピックタイトル>

| 内容 | 反映状況 |
| --- | --- |
| 初回 | [ ] |

> きっかけ: <この内容の元になった、ユーザーからの質問・要望>

<本文>
```

チェックボックス表は `#` 見出しの直後、本文プローズより前に置いてください。
`tools/check_memos.py` がこの位置を前提に構造を検査します。

**表の直後・本文より前には、必ず `> きっかけ: ...` を 1 行だけ書いてください。**
「ユーザーの何という質問・要望に対する説明か」を書く行です。質問をそのまま
引用しても、短く要約してもかまいません。これが無いメモは `check_memos.py`
がエラーにします（→ 9 節）。追記（→ 7 節）にも同じものが要ります。

## 6. 教材へ反映したときの操作

指示に従って教材本文へ反映したら、該当する行のチェックボックスを `[x]` にします。

**表の全行が `[x]` になったら**、ファイルごと `メモ/反映済/` へ移します。
一部の行だけ反映した（他の行がまだ `[ ]` のまま残っている）場合は、
`メモ/` に置いたままにします。

## 7. 反映済みのメモを後から更新する手順

`メモ/反映済/` にあるファイルをもとに、追記や更新をした方がよいと判断したら、
次の手順を必ず守ってください。

1. 対象ファイルを `メモ/反映済/` から `メモ/` へ**物理的に取り出す**
2. 冒頭の「初回」行のチェックは外さない（外す必要はありません）
3. **既存の本文を直接書き換えない**
4. ファイルの末尾に、日時付きのラベルを見出しの形で追記する

   ```
   <種別>_<YYYY>_<MMDD>_<HHMM>
   ---

   > きっかけ: <この追記の元になった、ユーザーからの質問・要望>

   <追記内容>
   ```

   `種別` は「追加」「修正」など。例: `追加_2026_0701_1015`、`修正_2026_0811_1038`。
   この 2 行はそのまま Markdown の setext 見出し（`## <label>`相当）としても解釈されます。
   **見出しの直後・追記内容より前には、初回と同じ形で `> きっかけ: ...` を書きます。**
   追記は初回と別の質問・要望がきっかけになっていることが多いので、都度書きます。
5. 冒頭のチェックボックス表に、追記したラベルと同じ文字列の行を追加する

   ```
   | 追加_2026_0701_1015 | [ ] |
   ```
6. その内容を教材へ反映したら、該当行だけ `[x]` にする
7. 表の全行が `[x]` になったら、再び `メモ/反映済/` へ戻す

## 8. 「未反映のものを全部反映して」と言われたとき

教材名の指定が無ければ、全教材の `メモ/` 配下（`反映済/` を除く）にある
すべてのファイルの、すべての `[ ]` 行が対象です。まとめて教材へ反映し、
反映した行を `[x]` にします。表の全行が `[x]` になったファイルは
`メモ/反映済/` へ移してください。

## 9. 検証: tools/check_memos.py

```bash
python tools/check_memos.py            # 全教材
python tools/check_memos.py rust       # 教材名で絞り込み（部分一致、複数可）
```

チェックする内容:

- チェックボックス表の書式（見出し行・区切り行・各行の形）が正しいか
- 表の 1 行目が文字どおり `初回` か、以降のラベルが `<種別>_<YYYY>_<MMDD>_<HHMM>` の書式か
- 表のラベルに重複が無いか
- 表にあるラベルに対応する追記見出しが本文にあるか（逆も）
- **初回の内容・各追記のそれぞれの直後に、`> きっかけ: ...` が書かれているか**
- **`メモ/反映済/` にあるファイルは、表の全行が `[x]` になっているか**

**未反映（`[ ]`）の行が存在すること自体はエラーにしません。** メモは反映を
強制されないものなので、溜まっていても正常な状態です。未反映の一覧は
情報として表示するだけです。

## 10. 命名・書式のクイックリファレンス

```
<教材名>/メモ/
├── README.md               検査対象外
├── 反映済/
│   ├── README.md           検査対象外
│   └── <反映済みのファイル>  全行 [x] が不変条件
└── <未反映・一部未反映のファイル>.md
```

```markdown
| 内容 | 反映状況 |
| --- | --- |
| 初回 | [x] |
| 追加_2026_0701_1015 | [ ] |
```

```
追加_2026_0701_1015
---

> きっかけ: <この追記の元になった、ユーザーからの質問・要望>

<追記内容>
```

`> きっかけ: ...` は、初回・追記それぞれの内容の先頭に必ず 1 行。
「ユーザーの何という質問・要望に対する説明か」を書く。
