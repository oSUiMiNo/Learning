# このリポジトリで作業するときに

技術教材を置くリポジトリです。教材を作る・直すなら、次の順で読んでください。

1. **[AUTHORING.md](AUTHORING.md) — 教材づくりの共通ルール**（分野を問わず効くこと）
2. **その教材の `<教材名>/AUTHORING.md`**（その教材で実際に踏んだこと）

教材を 1 冊作り終えたら、得られたナレッジを切り分けて**両方**を更新してください。

- `<教材名>/AUTHORING.md` を新しく書く（その教材でしか起きなかったこと）
- `AUTHORING.md`（共通ルール）を更新する（分野を問わず効くと分かったこと）

片方だけだと、共通のナレッジが教材フォルダに埋もれるか、
具体例が失われて自分ごとにならないかのどちらかになります。
切り分けの判定基準と手順は共通ルールの 8 節にあります。

とくに外せない要点は 2 つです。

- **本文の主張は、実物に対して実行して確かめてから書く。**
  Web の記事を信じて書いた早見表が 5 行間違っていた実例があります
  （複数あるバインディングのうち、別方の挙動を書いてしまっていた）
- **本文に載せるコードは書き写さない。** `examples/` の実ファイルを
  ビルド時に読み込む仕組みになっています

## 構成

- 各教材は `<教材名>/tools/build.py` で `docs/<教材名>/` を生成する
- 教材をまたぐもの（一覧ページ・旧 URL の転送）は `tools/build_hub.py`
- 章立ての情報源は `<教材名>/book/toc.py` だけ
- 公開前に走らせるものの一覧は `README.md` にある

`docs/` は生成物です。直接編集せず、ソースを直してビルドし直してください。

## Git 操作

- **作業ブランチから `main` へのマージは、都度の許可を取らずに進めてよい。**
  fast-forward できない（`main` 側に競合する変更がある）場合や、
  force push が必要になる場合は、通常どおり確認すること。
- push 前には検証（`README.md` に一覧がある公開前チェック）を通しておく。
