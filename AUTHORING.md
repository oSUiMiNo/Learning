# 教材づくりの共通ルール

このリポジトリで技術教材を作るときに、**分野を問わず効くこと**をまとめた文書です。

教材ごとの具体的な話（何に詰まったか、どう解決したか）は、
**その教材のフォルダにある `<教材名>/AUTHORING.md`** にあります
（まだ無い教材もあります）。分野が違えば担保のしかたも変わるので、
教材ごとに 1 冊ぶんずつ持たせる方式にしています。

この文書に書いてあることは、すべて**実際に教材を作る過程で痛い目を見て決まったもの**です。
一般論として書いているように見えても、元になった失敗が必ずあります。
納得できないルールは変えて構いませんが、変える前に理由を読んでください。

---

## 1. 最初に決めるべきたった 1 つのこと

**「この教材の正しさを、何によって担保するか」を、書き始める前に決めてください。**

技術教材が信用を失う原因は、文章の下手さではありません。
**書いてあるとおりにやったのに動かない**ことです。原因はほぼ 2 つに絞れます。

1. 本文のコードと、動作確認したコードが別物になっている（書き写す途中で壊れた）
2. 書いた人が古い情報・伝聞を信じていた

この 2 つは「気をつける」では防げません。仕組みで防ぎます。

### 答えるべき問い

> **この分野で「実物」とは何か。それをどうやって毎回同じ形で捕まえるか。**

| 分野 | 「実物」 | 捕まえ方 |
|---|---|---|
| GUI | 画面 | 仮想ディスプレイで起動して撮る |
| CLI・コンパイラ | 端末出力とエラーコード | 擬似端末で走らせて捕獲する |
| ライブラリの API | 実際の動作 | 取得してビルド・実行してみる |
| 設定ファイル・スキーマ | 通るか通らないか | 実際にそのツールに食わせる |

答えが出たら、**本文を 1 行も書く前に、それを最小構成で 1 本通してください**（→ 6 節）。
ここで詰まるなら教材の設計ごと変わります。

### 3 つの原則

どの教材も、この 3 つは共通で満たしています。

| 原則 | 防ぐもの |
|---|---|
| 本文のコードは `examples/` の実ファイルを**ビルド時に読み込む** | 書き写しによる劣化 |
| 素材（画面・出力）は**その同じファイルを実行して得る** | 図と実物のズレ |
| 本文が主張する挙動を**実物に対して実行して確かめる** | 古い情報・伝聞 |

3 つめが一番効きます。次の節で詳しく書きます。

> 「あとで検証を足す」はたいてい間に合いません。
> ただし**最小構成で先に 1 本通す**なら早すぎることはありません。
> 実際、ある教材では「掲載コード＝実ファイル」の照合を後から足しましたが、
> 足した直後に本物のズレを 2 件検出しました。

---

## 2. 情報の正しさをどう守るか

### 2-1. 「〜になる」と書きたくなったら、実際にそうさせてから書く

**これが最重要です。**

Qt6 の教材では、Web の解説記事を複数読んで書いた早見表が、**5 行間違っていました**。
「Qt6 ではこの書き方はエラーになる」と書いた項目が、実際には全部動いたのです。

原因は、その技術に Python バインディングが 2 つあり、
**片方の挙動をもう片方の話として書いていた**ことでした。
検索して出てくる記事は両者が混ざっているのに、本文はどちらの話か明示しないことが多い。

決めたこと:

- 実装・バインディング・ディストリビューションが**複数ある技術**では、
  記事がどれを対象にしているか必ず確認する。書いていないなら信用しない
- 「エラーになる」と書きたくなったら、**実際にエラーにさせてから書く**
- そしてこの種の主張は**機械検証に落とす**（→ 2-5）

### 2-2. バージョン・依存・ライセンスは一次情報から取る

同じ教材で「Python 3.9 以上」と書いていたものが、実際は **3.10 以上 3.14 以下**でした。
3.9 の読者は最初の `pip install` で失敗します。第 1 章で詰まるので実害は大きい。

正解の調べ方は簡単でした。パッケージのメタデータを読むだけです。

```python
import importlib.metadata as md
md.metadata("PySide6").get("Requires-Python")   # '<3.15,>=3.10'
```

対応バージョン・依存関係・ライセンスは、**「だいたいこのくらい」で書かない**。
必ずパッケージのメタデータや公式のリリース情報から取ってください。

そして取った一覧を、**自分で「最新」を判定しないでください。**

Rust の教材で、あるクレートの最新版を取り違えました。
レジストリの版一覧を取得し、**最終行を最新版として採った**のが原因です。
版の一覧は**公開時刻順であって、版の大小順ではありません**。
複数の系列を並行で保守しているものでは、新しい系列が出たあとに
旧系列の保守リリースが公開されるので、最終行は最新版になりません。

```
0.10.2     ← semver では最大
0.8.7
0.9.5      ← 最終行。これを採ると間違える
```

- 版の一覧を扱うなら、**必ず semver として比較して最大を採る**
- もっと確実なのは、**一覧を自分で解釈せず、そのツール自身に解決させる**こと
  （一時プロジェクトで依存追加コマンドを実行し、書き込まれた設定ファイルを読む）

詳しくは `rust/AUTHORING.md` の 2-1。

### 2-3. 「おまじない」は疑う

「この書き方が必要」と広まっている作法が、実は**もう不要**なことがあります。
初期バージョンの制約が記事に残り続けているケースです。

見た目に関わるものなら、**結果を突き合わせる**のが確実です。

```python
# 2 通りの書き方で描画し、画素が同じか比べる
a = hashlib.sha256(render("value")).hexdigest()
b = hashlib.sha256(render("enum")).hexdigest()
assert a == b       # 同じなら、その「必要」は嘘
```

### 2-4. 実測は「書くべきこと」の発見にも効く

検証は「書いたことの確認」だけの作業ではありません。

Qt6 の教材では、上の検証をしている最中に**本物の罠**が見つかりました。
ある API は「返す側」と「受け取る側」で値の型が違い、
素直に書くと**エラーも警告も出さずに、ただ動かない**というものでした。

> **API は両方向から触る。** 返す側／受け取る側、書く側／読む側。
> この種の非対称は、ドキュメントを読んでいるだけでは出てきません。

### 2-5. 主張を機械検証に落とす

本文が「実物はこう振る舞う」と主張しているなら、それを実行して確かめるスクリプトを書きます。

```python
def expect(claim: str, ok: bool, detail: str = "") -> None:
    print(f"  {'✓' if ok else '✗'} {claim}")
    if not ok:
        failures.append(f"{claim} — {detail}")

# 本文の主張を、そのまま実行可能な形で並べる
expect("QAction は QtWidgets から消え、QtGui にある", ...)
expect("短い書き方の enum は動くが、PyQt6 では動かない", ...)
```

食い違ったら非ゼロで終了させ、公開が止まるようにします。
上に書いた 5 行の間違いは、この仕組みがあれば最初から防げました。

> **副産物**: 訂正した結果、教材の内容はむしろ良くなりました。
> 「エラーになる／警告が出る／黙って動く」の 3 段階に整理し直したところ、
> **黙って動くもののほうが学習者には危険**という、元の原稿には無かった洞察が出てきたからです。
> 間違いを直すと、しばしば元より良くなります。

### 2-6. 公式ドキュメントも「書かれた時点」の情報

公式ドキュメントが読めない状況に置かれたことがあり（取得が弾かれた）、
代わりに**インストール済みの実物を直接調べる**しかありませんでした。

結果的にそのほうが確実でした。**手元で動いているものが最新の一次情報です。**

---

## 3. 素材づくりの共通則

### 3-1. 教材専用の細工は、サンプルの「外」に置く

最初にやりがちなのが、素材を取るためのヘルパをサンプルから import させることです。
**これをやると、読者がコピーしてそのまま動かせなくなります。**

正解は、サンプルを一切変えずに**外から実行を乗っ取る**ことです。

```python
# サンプルは、自分が撮影されていることを知らない
original_exec = Application.exec

def patched_exec():
    schedule_capture()
    return original_exec()

Application.exec = staticmethod(patched_exec)
runpy.run_path(str(script), run_name="__main__")
```

**「教材のための細工は、教材のツール側に寄せる」** — これは言語も分野も問いません。

### 3-2. 決定性を壊すものを潰す

素材は**毎回同じ結果になる**ことが前提です。壊す典型は次の 3 つ。

| 壊すもの | 対処 |
|---|---|
| 保存機能（前回の実行結果を読む） | 保存先を撮影ごとの使い捨てディレクトリに向ける |
| 時刻・乱数 | 固定するか、素材に写り込ませない |
| 環境（デスクトップ背景、ロケール、フォント） | 明示的に指定する。写り込むものは自分で用意する |

```python
env["XDG_DATA_HOME"] = str(throwaway_dir)   # 状態の置き場所を外から差し替える
```

状態を持つサンプルは、**状態の置き場所を外から差し替えられる**ようにしておいてください。

### 3-3. 装飾は素材に焼き込まない

スクリーンショットにウィンドウ枠を合成しようとしてやめました。
**HTML/CSS 側で描くほうが、あらゆる面で良かった**からです。

- 明暗テーマに追従する
- 文字が選択・検索できる
- 素材が小さくなる
- 撮り直しても装飾のデザインは変わらない

素材は素材のまま取り、装飾は表示側で。
装飾に必要な情報（タイトル、サイズなど）だけを JSON で受け渡します。

### 3-4. 生成物は 1 つずつ目で見る

自動検証は「壊れていない」ことしか言いません。
**「意図したものが写っているか」は人間が見るしかない**です。

空の画面、文字化け、見切れ、想定と違う状態。これらは検証を全部通ります。

### 3-5. 実測できるからといって、実測として載せてよいとは限らない

**素材が正しくても、読者の環境と食い違うなら、それは読者にとって嘘です。**

Rust の教材は Linux 上で素材を作り、読者は Windows で読みます。
ツールチェインの構成を表示するコマンドは実際に走らせられますが、
出てくるのは `x86_64-unknown-linux-gnu` でした。実測ではありますが、載せれば誤情報です。

環境に依存して写り込むもの（パス、ホスト名、既定のターゲット、ロケール、
ユーザ名）を含む素材は、この判断が要ります。決めたのは次の 2 つです。

- **素材を「実測」と「再現」の 2 種類に分け、見た目で区別できる印を必ず付ける。**
  再現には**何に基づくかを必ず添える**（公式のソースにある表示文字列など。想像で書かない）
- **印の付け忘れが起きない構造にする。** 差し込む指示子そのものを分け、
  印の無い素材が 1 つでもあれば検証で止める

```
<!--term:  ... -->   実測の印が自動で付く
<!--termx: ... -->   再現の印と出典が自動で付く
```

実測の素材に手を入れる場合も、**どこまで直したかを教材本文に明記してください。**
Rust の教材ではパスの表記だけを Windows 形式に直し、
エラー文面・版番号・所要時間には触れないと決めて、それを README と第 0 章に書きました。
ここが曖昧だと「実測」という言葉自体が意味を失います。

---

## 4. サイト生成で共通して踏む罠

各教材の `tools/build.py` は同じ発想で作られているので、
ここに書いた罠は**どの教材でも踏みます**。

### 4-1. 構文強調の出力を行単位に分けるとき

行番号を付けるため、構文強調ツールの出力を 1 行ずつ要素で包む必要があります。
単純に改行で分割すると壊れます。**文字列リテラルやコメントは複数行にまたがる**ためです。
開いたままのタグを行末で閉じ、次の行で開き直す処理が要ります。

```python
# 行末: 開いているタグを全部閉じる → 次の行頭で同じものを開き直す
lines.append(f'<span class="cl">{"".join(current)}{"</span>" * len(open_tags)}</span>')
```

### 4-2. `<pre>` の中でブロック要素を改行文字で繋ぐと、行間が 2 倍になる

各行を `display: block` にしたうえで改行文字でも繋ぐと、
すべてのコードが間延びします。**どちらか一方だけにする。**

### 4-3. 空行が消える（2 つの別問題）

- **見た目**: 中身のない行は高さが 0 になり、行番号が重なる
  → `min-height` を行の高さと同値で入れる
- **コピー**: `innerText` は中身のないブロックを飛ばすため、**コピーすると空行が落ちる**
  → 行の要素から組み立て直す

```javascript
const lines = code.querySelectorAll('.cl');
const text = Array.from(lines).map(el => el.textContent).join('\n');
```

この 2 つは**気づかないまま公開してしまいやすい**種類の不具合です。
だから次の照合を入れています。

### 4-4. 掲載コードと実ファイルの一致を機械で照合する

「本文のコードは実ファイルを読み込んでいる」だけでは足りません。
**表示に至るまでの経路（強調・分割・エスケープ）で壊れる**からです。

```javascript
// ブラウザ上で実際に表示されている文字列を組み立て、実ファイルと突き合わせる
document.querySelectorAll('.code-block[data-src]').forEach(fig => {
  out[fig.dataset.src] = Array.from(fig.querySelectorAll('code .cl'))
    .map(el => el.textContent).join('\n');
});
```

1 バイトでも違えば落とします。

### 4-5. ブラウザ検証まわり

| 症状 | 原因と対処 |
|---|---|
| 要素スクショに画像が写らない | 遅延読み込みで未読込。`loading` を `eager` にして全画像の完了を待つ |
| 検証を 2 つ走らせると片方が落ちる | 固定ポートの衝突。空きポートを OS に選ばせる |
| 後片付けのはずが自分ごと死ぬ | `pkill -f "http.server -d"` は自分のコマンドラインにも一致する。ポート単位で止める |

### 4-6. 検証で最低限見るもの

- JavaScript のコンソールエラー
- 404（画像・リンク）
- 横スクロールの発生（スマホ幅を含む）
- 展開されていない指示子・「未作成」の目印
- **掲載コードと実ファイルの一致**
- 明暗 2 テーマ × デスクトップ／スマホ幅
- **検査した件数**（次項の理由）

### 4-7. 出力先を変えたら、同じ場所を参照している全部を洗う

教材が 2 冊目になったとき、公開先を `docs/` から `docs/<教材名>/` に移しました。
このとき**生成スクリプトだけを直し、兄弟スクリプトを取り残しました。**

| 取り残したもの | 症状 |
|---|---|
| 素材を撮るスクリプト | 旧パスに出力する。**撮り直した瞬間に素材が見つからなくなる** |
| ブラウザ検証スクリプト | 巡回対象が別のページ群になり、**対象を 1 つも検証していなかった** |

2 つめが怖いところです。**検証スクリプトが壊れても「問題はありませんでした」と言います。**
落ちてくれないので気づけません。

- 出力先を変えたら `grep -rn 'docs' <教材>/tools/` で参照箇所を全部洗う
- **検証スクリプトは、何件を検査したかを必ず出力する。**
  「16 ページを検査し」と出しておけば、数が変わったときに人間が気づけます

---

## 5. 書き方・説明の仕方

### 5-1. 「エラーになる」と「黙って動く」を区別する

学習者にとって危険なのは前者ではなく**後者**です。
エラーは気づけますが、黙って動くものは間違いに気づく機会がありません。

早見表を作るなら、この 3 段階で分けてください。

- ❌ エラーになる
- ⚠️ 動くが警告が出る
- 😐 黙って動く（**いちばん危険**）

### 5-2. 症状から引ける索引を作る

学習者は「原因」ではなく「症状」しか持っていません。
「ウィンドウが一瞬で消える」「押しても何も起きない」から引ける表を用意すると、
教材が**読み物からリファレンスに変わります**。

### 5-3. 図は「見た目」と「コード構造」を並べる

概念図でいちばん効いたのは、
**完成イメージと、それを作るコードの構造を左右に並べた図**でした。
1 対 1 で対応していることが目で分かると、書き方が腑に落ちます。

### 5-4. 環境差は先に言う

素材の見た目や挙動が読者の環境と違うなら、**最初に明言してください**。
書かないと、読者は「自分の環境がおかしいのでは」と疑い始めます。

### 5-5. 自分の間違いを教材に書く

間違いを訂正したら、その経緯を短く本文にも書いてください。

> 実を言うと、この教材の初稿ではここを「エラーになる」と書いていました。
> 別のバインディングの挙動を、こちらの話として書いてしまっていたのです。

**隠すより書いたほうが、教材の信頼性は上がります。**
「なぜその情報が正しいと言えるのか」を読者が判断できるようになるからです。

### 5-6. 分量と区切り

1 章あたり 5〜10 分で読み切れる量に割ってください。
章末に「まとめ」と「やってみよう」を必ず置くと、読者が区切りを付けやすくなります。

### 5-7. 躓きは、そのまま教材のネタになる

作る過程で自分が詰まったことは、読者も詰まります。
**回避策をコードに入れて終わりにせず、なぜ必要なのかを本文で説明してください。**

### 5-8. 読者に「既に知っている技術」があるなら、対応表より裏切られる箇所を書く

読者層を「別の言語・ツールの経験者」に定めた教材では、
対応表を作りたくなります。しかし**対応表は調べれば分かります。**

効くのは、**似ているのに違うものを名指しする**ほうです。
知っているつもりで書いて、しかも気づきにくい形で失敗するからです。

Rust の教材（C# 経験者向け）で効いたのは、たとえばこれでした。

> C# の `Task` は呼んだ時点で走る。Rust の `Future` は **await されるまで 1 行も走らない**。
> C# の癖のまま「呼んでおいて後で await」と書くと、
> **並行になっているつもりで直列になる。エラーは出ない。**

対応表しか無いと、読者はこの違いを踏み抜きます。
専用の囲みを 1 種類作り、各章で名指しする形にしました。

**同じ道具が同じ名前で違う動きをする箇所を、先に洗い出してください。**
それがその教材でいちばん価値のある部分になります。

---

## 6. 進め方の順番

この順でやると手戻りが少なかった、という順番です。

1. **実現可能性を、いちばん怪しいところから確かめる。**
   本文を 1 行も書く前に、素材が 1 つ取れるところまでやる。
   ここで駄目なら教材の設計ごと変わります
2. **骨組みとビルドの仕組みを作る**（章立ての定義、生成スクリプト、スタイル）
3. **サンプルを 2〜3 本だけ書いて、パイプラインを最後まで通す。**
   量産する前に、生成されたページをブラウザで目視する
4. **残りのサンプルを書き、検証を通す**
5. **素材を作り、1 つずつ目で確かめる**
6. **本文を書く**
7. **全体をビルドしてブラウザで検証する**

> **3 が特に重要です。** 30 本書いてから仕組みの欠陥に気づくと、全部やり直しになります。

---

## 7. 新しい教材を作るときのチェックリスト

```
□ 正しさの担保を決めた（この分野で「実物」とは何か／どう毎回同じ形で捕まえるか）
□ いちばん怪しい部分の実現可能性を、着手前に確かめた
□ 本文のコードは実ファイルを読み込んでいる（書き写していない）
□ 掲載コードと実ファイルの一致を、機械で照合している
□ サンプルは全部、単体で実行できる（教材専用のヘルパを import していない）
□ サンプルは全部、自動で実行検証されている
□ 「〜になる」という挙動の主張は、実物で確かめてから書いた
□ 対応バージョン・依存・ライセンスは一次情報から取った
□ 「最新版」を一覧の最終行で判定していない（semver で比較したか、ツールに解決させた）
□ 素材は毎回同じ結果になる（状態・時刻・環境を持ち込んでいない）
□ 読者の環境と食い違う素材を「実測」として載せていない（実測と再現を印で区別した）
□ 検証スクリプトが「何件を検査したか」を出力している
□ 生成物を 1 つずつ目で見た（自動検証は「壊れていない」しか言わない）
□ 明暗テーマ・スマホ幅で崩れないか確認した
□ 症状から引ける索引がある
□ 環境差（見た目・バージョン）を本文で明言した
□ 検証環境のバージョンをページに表示している（手書きせず実測値を出す）
□ <教材名>/AUTHORING.md を残した（その教材でしか起きなかったこと）
□ 共通編 AUTHORING.md を更新した（分野を問わず効くと分かったこと）  ← 忘れやすい
```

最後の 2 つはセットです。片方だけでは次の教材に活きません（→ 8 節）。

---

## 8. 教材を作り終えたら、手引きを 2 つとも更新する

**教材を 1 冊作ったら、次の 2 つを必ずセットで行ってください。**

1. **`<教材名>/AUTHORING.md` を新しく書く** — その教材でしか起きなかったこと
2. **この共通編（`AUTHORING.md`）を更新する** — 分野を問わず効くと分かったこと

片方だけでは駄目です。理由は次のとおり。

- **固有編だけ書くと**、共通のナレッジがその教材のフォルダに埋もれます。
  次の教材を作る人は、無関係に見えるフォルダの中まで読みに来ません。
  同じ失敗が繰り返されます
- **共通編だけ書くと**、具体例が失われます。
  「実物で確かめよう」だけ読んでも、何をどう間違えたのかが分からず、
  自分ごとになりません

実際、この共通編の内容はすべて Qt6 の教材を作る過程で得られたものです。
1 冊目の時点で切り分けておかなければ、2 冊目は同じ穴に落ちていました。

### 8-1. どちらに書くかの判定

書き出した項目それぞれに、この問いを当ててください。

> **次に別の分野の教材を作るとき、同じことをまた書くか？**

**Yes なら共通編。No なら固有編。** 迷ったら、次の 2 つで補助判定します。

| テスト | 判定 |
|---|---|
| **固有名詞を消しても意味が通るか**（言語名・ツール名・クラス名を伏せる） | 通る → 共通編の候補 |
| **原因がその教材だけの事情か、教材群の仕組みにあるか** | 仕組み側 → 共通編 |

2 つめが効いた例があります。
「構文強調の出力を行単位に分けるときの罠」は Qt6 の教材で見つかりましたが、
原因は Qt ではなく**各教材が同じ発想で持っている `tools/build.py`** にありました。
だから共通編にあります。次の教材でも同じ場所を踏むからです。

逆に「モーダルダイアログを撮ると `click()` が戻らない」は、
GUI とスクリーンショットでしか起きません。固有編です。

### 8-2. 分け方の実際

**両方に書く**のが普通です。同じ話を、粒度を変えて置きます。

| | 共通編 | 固有編 |
|---|---|---|
| 内容 | 原則・判断基準・やり方の型 | 実際の症状・原因・コード片 |
| 粒度 | 「装飾は素材に焼き込まない」 | 「ウィンドウ枠を CSS で描き、タイトルとサイズだけ JSON で渡す」 |
| 固有名詞 | 出さない | 出す |

共通編には、**根拠として 1〜2 行だけ実例を残してください**。
原則だけだと説得力がなく、読み飛ばされます。
詳細は固有編に置き、必要なら「詳しくは `<教材名>/AUTHORING.md`」と添えます。

### 8-3. 手順

固有編を先に書いてから、共通編に昇格させるものを選ぶ順がやりやすいです。

1. **まず固有編に全部書き出す。** 選別せず、起きたことをそのまま記録する
2. **各項目に 8-1 の問いを当てる**
3. **Yes のものを共通編へ昇格させる。**
   固有編には具体例と症状を残し、原則は共通編に移す
4. **共通編は、まず既存の節への追記で済ませられないか考える。**
   新しい節を増やすのは最後の手段です（膨らむと読まれなくなります）
5. **既存の記述とぶつかったら、両方が成り立つように書き直す。**
   「分野によってはこうなる」と条件を明示する形が多い

### 8-4. 共通編を膨らませないために

- **1 教材でしか起きていない話を、慌てて共通化しない。**
  「たぶん他でも起きる」で上げると、根拠のない一般論が溜まります。
  明らかな一般則でないなら、2 冊目で同じことが起きてから昇格させれば十分です
- **既存の項目と統合できるなら統合する。** 似た話を並べない
- **具体例は 1〜2 行に切り詰める。** 詳細は固有編にあります

### 8-5. 固有編の推奨構成

```
1. この教材で「実物」をどう捕まえたか
   担保の設計と、なぜその方法を選んだか。ツールごとの担保対象の一覧

2. やらかした間違い
   何を書いて、実際は何だったか、どう気づいたか、そこから何を決めたか

3. 分野固有の詰まりどころ
   症状 → 原因 → 対処。コード片つきで

4. 環境の落とし穴
   その言語・ツールチェーン特有のもの

5. 説明で工夫したこと
   その分野ならではの、伝え方の発見

6. この教材の構成とツール
   README への導線と、間違えやすい設定
```

書くときの目安:

- **一般論を書かない。** 「テストは大事」ではなく「何を書いて、どう間違っていたか」
- **症状から書く。** 読み手は原因を知らない状態で来ます
- **コード片を入れる。** 説明だけだと再現できません
- **成功例より失敗例。** うまくいった設計だけ書いても、なぜそうするかが伝わりません
- **冒頭で共通編へ誘導する。** 固有編だけ読まれると前提が抜けます

---

## 9. リポジトリの決まり

```
Learning/
├── docs/              GitHub Pages で公開されるサイト（生成物。直接編集しない）
├── <教材名>/
│   ├── book/toc.py        章立ての唯一の情報源
│   ├── examples/          コードの唯一の情報源
│   ├── assets/            スタイルと図版
│   ├── tools/             ビルドと検証
│   ├── README.md          構成と使い方
│   └── AUTHORING.md       この教材を作って分かったこと
├── tools/build_hub.py 教材一覧と旧 URL の転送
├── AUTHORING.md       この文書（共通ルール）
└── README.md          公開前に走らせるものの一覧
```

- 各教材は `<教材名>/tools/build.py` で `docs/<教材名>/` を生成する
- 教材をまたぐもの（一覧ページ、旧 URL の転送）は `tools/build_hub.py` が担当する
- 教材を 1 冊足したら `tools/build_hub.py` の `BOOKS` に 1 項目加える
- ナビ・目次・前後リンクはすべて `book/toc.py` から生成される。手で書かない
- **担保のしかたは教材ごとに違ってよい。** 共通しているのは
  「本文の主張を、実物に対して機械で確かめている」という一点だけです
