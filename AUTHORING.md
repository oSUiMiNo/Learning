# 教材づくりの手引き

このリポジトリで技術教材を作るときの、実地のメモです。
Qt6 Widgets 入門（全 15 章）を 1 冊まるごと作った過程で、
**実際に詰まったこと・実際に間違えたこと**だけを書いています。
一般論は書きません。

読み手として想定しているのは、次に別のセッションで教材を作る人（または AI エージェント）です。
「なぜそうなっているのか」が分かるように書いたので、
納得できないルールは変えてもらって構いません。ただし変える前に理由を読んでください。

---

## 1. 最初に決めるべきたった 1 つのこと

**「この教材の正しさを、何によって担保するか」を最初に決めてください。**
書き始めてからでは間に合いません。

技術教材が信用を失う原因は、文章の下手さではありません。
**書いてあるとおりにやったのに動かない**ことです。そしてその原因はほぼ 2 つです。

1. 本文のコードと、動作確認したコードが別物になっている（書き写す途中で壊れた）
2. 書いた人が古い情報を信じていた

この 2 つは「気をつける」では防げません。仕組みで防ぐ必要があります。
Qt6 の教材では次の 3 つを置きました。

| 仕組み | 防ぐもの |
|---|---|
| 本文のコードは `examples/` の実ファイルを**ビルド時に読み込む** | 書き写しによる劣化 |
| スクリーンショットは**その同じファイルを実行して撮る** | 図と実物のズレ |
| 本文が主張する挙動を**実物に対して実行して確かめる** | 古い情報・伝聞 |

3 つめが一番効きました。詳しくは次の節で書きます。

> **教訓**: 「あとで検証を足す」は、たいてい間に合いません。
> ただし**最小構成で先に 1 本通す**なら早すぎることもありません。
> 実際、この教材でも「掲載コード＝実ファイル」の照合は後から足しましたが、
> 足した直後に本物のズレを 2 件検出しました。

---

## 2. 実際にやらかした間違い

ここが一番読む価値のある節だと思います。全部このセッションで実際に起きたことです。

### 2-1. Web の記事を信じて書いた早見表が、5 行間違っていた

第 13 章に「Qt5 の書き方は Qt6 ではこうエラーになる」という表を作りました。
Web の解説記事を複数読んで書いたので、自信もありました。

実物で試したら、**5 行が間違っていました。**

| 書いたこと | 実際 |
|---|---|
| `app.exec_()` は `AttributeError` | 動く（`DeprecationWarning` は出る） |
| `Qt.AlignCenter` は `AttributeError` | **動く。警告すら出ない** |
| `Qt.Horizontal` / `Qt.Checked` は `AttributeError` | 動く |
| `QMessageBox.Yes` は `AttributeError` | 動く（`==` 比較も成立する） |

原因は単純で、**PyQt6 の挙動を PySide6 の話として書いていた**からです。
Qt には Python バインディングが 2 つあり、
片方（PySide6）は古い書き方を黙って受け付ける「寛容モード」を持ち、もう片方（PyQt6）は持ちません。
検索して出てくる記事は両者が混ざっているのに、本文はどちらの話か明示しないことが多い。

**ここから決めたこと:**

- 実装・バインディング・ディストリビューションが複数ある技術では、
  **記事がどれを対象にしているか**を必ず確認する。書いていないなら信用しない
- 「エラーになる」と書きたくなったら、**必ず実際にエラーにさせてから書く**
- そして、この種の主張は**機械検証に落とす**（→ `qt6-widgets/tools/check_claims.py`）

`check_claims.py` は早見表の各行を実物に対して実行し、食い違えば非ゼロで終了します。
この誤りは、このスクリプトがあれば最初から防げました。

> **副産物**: 訂正した結果、教材の内容はむしろ良くなりました。
> 「エラーになる／警告が出る／黙って動く」の 3 段階に整理し直したところ、
> **黙って動くもののほうが学習者には危険**（古い書き方が身についてしまう）
> という、元の原稿には無かった洞察が出てきたからです。
> 間違いを直すと、しばしば元より良くなります。

### 2-2. 対応バージョンを想像で書いた

「Python 3.9 以上」と書いていましたが、実際は **3.10 以上 3.14 以下**でした。
3.9 の読者は `pip install` の時点で失敗します。第 1 章で詰まるので、実害は大きい。

正解の調べ方は簡単でした。パッケージのメタデータを読むだけです。

```python
import importlib.metadata as md
md.metadata("PySide6").get("Requires-Python")   # '<3.15,>=3.10'
```

> **教訓**: 対応バージョン・依存関係・ライセンスは、**必ず一次情報（パッケージのメタデータや
> 公式のリリース情報）から取る**。「だいたいこのくらい」で書かない。
> ライセンスも `importlib.metadata` や PyPI の JSON API から取れます。

### 2-3. 「必要」と書いたことが、実は不要だった

モデル/ビューの章で、次のように書いていました。

- `TextAlignmentRole` は `int(...)` で包んで返すこと
- `CheckStateRole` は `.value` を付けて返すこと

どちらも**現行バージョンでは不要**でした。実際に両方の書き方でウィジェットを描画させ、
**画像のハッシュを比較して同一**であることを確認しました。

これらは初期バージョンで列挙型の扱いが不安定だった頃の慣習が、記事に残り続けていたものです。

> **教訓**: 「おまじない」として広まっている書き方は疑う。
> 見た目に関わるものなら**描画結果を突き合わせる**のが確実です。
>
> ```python
> # 2 通りの書き方でウィジェットを描画し、画素が同じか比べる
> a = hashlib.sha256(render("value")).hexdigest()
> b = hashlib.sha256(render("enum")).hexdigest()
> assert a == b
> ```

### 2-4. 逆に、実測でしか見つからない「本物の罠」も出てきた

上を検証している最中に、こういうものが見つかりました。

`data()` からは列挙型をそのまま返してよい。ところが `setData()` に**渡ってくる**値は
列挙型ではなく `int` です。したがって次のコードは、**エラーも警告も出さずに、ただ動きません**。

```python
self._items[index.row()]["done"] = (value == Qt.CheckState.Checked)   # 常に False
```

`2 == Qt.CheckState.Checked` が `False` であることを実際に確認して、
「チェックしても完了にならない」という症状として本文に書きました。

> **教訓**: 実測は「書いたことの確認」だけでなく、**書くべきことの発見**にも効きます。
> API を両方向（返す側／受け取る側）から触ってみると、この種の非対称が出てきます。

---

## 3. 技術的に詰まった点（撮影・実行まわり）

### 3-1. 撮影ハーネスはサンプルの「外」に置く

最初、スクリーンショット撮影用のヘルパを作り、サンプルから import させました。
すぐに捨てました。**サンプルに教材専用のコードが混ざると、読者がコピーして動かせなくなる**からです。

正解は、サンプルを一切変えずに**外から実行を乗っ取る**ことでした。

```python
# tools/_capture.py — サンプルは自分が撮影されていることを知らない
original_exec = QApplication.exec

def patched_exec():
    QTimer.singleShot(delay, capture)     # 描画が落ち着いた頃に撮る
    return original_exec()

QApplication.exec = staticmethod(patched_exec)
runpy.run_path(str(script), run_name="__main__")
```

これで `examples/*.py` は完全に素のままです。読者がそのままコピーして動かせます。

**この方針は言語を問わず有効です。** 「教材のための細工は、教材のツール側に寄せる」。

### 3-2. `QApplication.exec` は静的メソッドだった

上のコードで `patched_exec(self)` と書いて `TypeError: exec() takes no arguments (1 given)` に遭いました。
`staticmethod` で包む必要があります。

> **教訓**: メソッドを差し替えるときは、まず**それが静的か・インスタンスメソッドか**を確認する。

### 3-3. モーダルダイアログを撮ろうとすると固まる

ダイアログを開いた状態を撮りたくて「ボタンを押す → 少し待つ → 撮る」と直列に繋いだところ、
撮影までたどり着きませんでした。

**モーダルダイアログを開くボタンの `click()` は、ダイアログが閉じるまで戻ってこない**からです。
直列に繋ぐと、その次の処理が永遠に始まりません。

解決は、**操作を絶対時刻で予約する**ことでした。

```python
at = settle
for step in steps:
    QTimer.singleShot(at, make_action(step))   # 直列に繋がず、それぞれ独立に予約
    at += step.get("ms", 250)
QTimer.singleShot(at + 250, capture)           # 撮影も独立に予約
```

タイマーは入れ子のイベントループの中でも発火するので、これで動きます。

さらに、**モーダルが開いていると `quit()` では終われません**。
開いているウィンドウを全部閉じてから止める必要があります。

```python
for window in visible_windows():
    window.close()
QApplication.instance().quit()
```

### 3-4. 複数ウィンドウを撮ると、背景の黒が写り込む

ダイアログと親ウィンドウを一緒に撮るため画面全体を取り込んだところ、
仮想ディスプレイの黒い背景が額縁のように写りました。

解決は、**画面を撮るのをやめて、各ウィンドウを個別に取り込み、自前で並べ直す**ことでした。

```python
canvas.fill(QColor("#cdc9c2"))            # 中立的な下地を自分で用意する
for offset, pixmap in shots:              # 手前に来るべきものを最後に描く
    painter.drawPixmap(offset, pixmap)
```

> **教訓**: 環境（デスクトップ背景、ウィンドウマネージャの有無）が写り込むと、
> 教材の見た目が環境依存になります。**写り込むものは自分で用意する。**

### 3-5. 2 倍解像度は「どちらの座標系か」を間違えやすい

高解像度ディスプレイ向けに 2 倍で撮るとき、切り出し範囲に
**すでに倍率がかかった値**を渡してしまい、余白だらけの画像ができました。

多くの API は「拡大前の座標」を受け取り、返る画像が「拡大後の画素数」になります。
**倍率を二重にかけない。**

### 3-6. 決定性を壊すもの: 保存機能

保存機能のあるサンプル（ToDo アプリ）は、前回の実行結果を読み込むため、
撮影のたびに違う画面になりました。

撮影ごとに保存先を使い捨てのディレクトリに向けて解決しました。

```python
env["XDG_DATA_HOME"] = str(throwaway_dir)
```

> **教訓**: 撮影・検証は**毎回同じ結果になる**ことが前提です。
> 状態を持つサンプルは、状態の置き場所を外から差し替えられるようにしておく。

### 3-7. `runpy.run_path` は import パスを通してくれない

隣のサンプルを import するサンプル（`ch10_filter.py` が `ch10_tableview` を使う）が
`ModuleNotFoundError` で落ちました。
直接 `python foo.py` で動くのに、ハーネス経由だと動かない、という分かりにくい形で出ます。

```python
sys.path.insert(0, str(script.parent))   # 直接実行したときと同じ状態にする
```

### 3-8. サンプル同士を import させるなら、実行部をガードする

上の件を直したら、今度は import した側のアプリまで起動しました。
モジュール直下に実行コードが置いてあったからです。

```python
if __name__ == "__main__":     # import されたときは動かさない
    app = QApplication(sys.argv)
    ...
```

> これは教材としても良い題材になりました。
> 「なぜこれが要るのか」を本文で説明する自然な機会になります。
> **躓きは、しばしばそのまま教材のネタになります。**

---

## 4. 技術的に詰まった点（サイト生成まわり）

### 4-1. 構文強調の出力を行単位に分けるとき

行番号を付けるため、構文強調ツールの出力を 1 行ずつ要素で包む必要がありました。
単純に改行で分割すると壊れます。**文字列リテラルやコメントは複数行にまたがる**ため、
開いたままのタグを行末で閉じ、次の行で開き直す処理が要ります。

```python
# 行末: 開いているタグを全部閉じる → 次の行頭で同じものを開き直す
lines.append(f'<span class="cl">{"".join(current)}{"</span>" * len(open_tags)}</span>')
```

### 4-2. `<pre>` の中でブロック要素を改行文字で繋ぐと、行間が 2 倍になる

各行を `display: block` にしたうえで改行文字でも繋いでいたため、
すべてのコードが間延びしていました。**どちらか一方だけにする。**

### 4-3. 空行が消える（2 つの別問題）

- **見た目**: 中身のない行は高さが 0 になり、行番号が重なる → `min-height` を行の高さと同値で入れる
- **コピー**: `innerText` は中身のないブロックを飛ばすため、**コピーすると空行が落ちる**

コピーは、行の要素から組み立て直すのが確実でした。

```javascript
const lines = code.querySelectorAll('.cl');
const text = Array.from(lines).map(el => el.textContent).join('\n');
```

> この 2 つは、**気づかないまま公開してしまいやすい**種類の不具合です。
> 「掲載コードが実ファイルと 1 バイトも違わないか」を自動で照合するようにして、
> 二度と起きないようにしました。

### 4-4. ウィンドウ枠は画像に焼き込まない

スクリーンショットにタイトルバーを合成しようとしましたが、やめました。
**HTML/CSS 側で描くほうが、あらゆる面で良かった**からです。

- 明暗テーマに追従する
- タイトル文字が選択・検索できる
- 画像が小さくなる
- 撮り直しても枠のデザインは変わらない

撮影時に「実際のウィンドウタイトルとサイズ」だけを JSON に書き出し、生成側が読んでいます。

> **一般化**: **見た目の装飾は画像に含めない。** 素材は素材のまま撮り、装飾は表示側で。

### 4-5. ブラウザ検証で引っかかったこと

- **遅延読み込みの画像が写らない**: 要素単位のスクリーンショットでは画面外の画像が未読込。
  `loading` を `eager` に変えて、全画像の読み込み完了を待ってから撮る
- **固定ポートは同時実行で衝突する**: 検証を 2 つ走らせたら片方がもう片方のサーバを止めた。
  空きポートを OS に選ばせる
- **`pkill -f "http.server -d"` は自滅する**: パターンが自分自身のコマンドラインにも一致する。
  ポート単位で止めるか、パターンを工夫する

---

## 5. 書き方・説明の仕方

### 5-1. 「エラーになる」と「黙って動く」を区別する

これが今回いちばんの発見です。学習者にとって危険なのは前者ではなく**後者**です。
エラーは気づけますが、黙って動くものは間違いに気づく機会がありません。

早見表を作るなら、この 3 段階で分けてください。

- ❌ エラーになる
- ⚠️ 動くが警告が出る
- 😐 黙って動く（**いちばん危険**）

### 5-2. 症状から引ける索引を作る

学習者は「原因」ではなく「症状」しか持っていません。
「ウィンドウが一瞬で消える」「押しても何も起きない」から引ける表を用意すると、
教材が**読み物からリファレンスに変わります**。

### 5-3. 図は「見た目」と「コード構造」を並べる

概念図でいちばん効いたのは、**画面のスケッチと、それを作るコードの木構造を左右に並べた図**でした。
1 対 1 で対応していることが目で分かると、書き方が腑に落ちます。

### 5-4. 環境差は先に言う

スクリーンショットの見た目が読者の環境と違うなら、**最初に明言してください**。
今回は全スクリーンショットを OS 非依存のスタイルに固定し、
第 2 章で「あなたの環境では配色が違うが、構造とコードは同じ」と書きました。
これを書かないと、読者は「自分の環境がおかしいのでは」と疑い始めます。

### 5-5. 自分の間違いを教材に書く

早見表を訂正したあと、その経緯を教材本文にも短く書きました。

> 実を言うと、この教材の初稿では 😐 の行を「エラーになる」と書いていました。
> PyQt6 の挙動を PySide6 の話として書いてしまっていたのです。

**隠すより書いたほうが、教材の信頼性は上がります。**
「なぜその情報が正しいと言えるのか」を読者が判断できるようになるからです。

### 5-6. 分量の目安

1 章あたり 5〜10 分で読み切れる量に割ってください。
章末に「まとめ」と「やってみよう」を必ず置くと、読者が区切りを付けやすくなります。

---

## 6. 進め方の順番

この順でやると手戻りが少なかった、という順番です。

1. **実現可能性を、いちばん怪しいところから確かめる。**
   今回は「そもそもスクリーンショットが撮れるのか」でした。
   本文を 1 行も書く前に、小さなウィンドウを 1 枚撮るところまでやりました。
   ここで駄目なら教材の設計ごと変わるからです
2. **骨組みとビルドの仕組みを作る**（章立ての定義、生成スクリプト、スタイル）
3. **サンプルを 2〜3 本だけ書いて、パイプラインを最後まで通す。**
   量産する前に、生成されたページをブラウザで目視する
4. **残りのサンプルを書き、検証を通す**
5. **素材（画像・図）を作り、1 枚ずつ目で確かめる**
6. **本文を書く**
7. **全体をビルドしてブラウザで検証する**

> **3 が特に重要です。** 30 本書いてから仕組みの欠陥に気づくと、全部やり直しになります。

---

## 7. 環境まわりの落とし穴

この実行環境で実際に踏んだものです。似た環境ならまた踏みます。

| 症状 | 原因と対処 |
|---|---|
| GUI ライブラリの import が `libEGL.so.1` で失敗 | OS 側の共有ライブラリ不足。`apt-get update` してから入れる（update を飛ばすと 404 で失敗する） |
| 公式ドキュメントサイトの取得が 403 | 一部のサイトは自動取得を弾く。検索結果と**実物の introspection** で補う |
| 日本語が豆腐になる | フォントの有無を先に確認する（`fc-list :lang=ja`） |
| ウィンドウに枠がない・必ず原点に出る | 仮想ディスプレイにウィンドウマネージャがいないため。**むしろ好都合**（位置が決定的になる） |
| C++ をビルドしようとしてヘッダがない | Python バインディングにはライブラリ本体は入っているがヘッダは入っていない。開発パッケージを別途入れる |

**403 の件は、結果的に良い方向に働きました。**
公式ドキュメントが読めなかったので、代わりに**インストール済みの実物を直接調べる**しかなく、
それが「ドキュメントより確実な一次情報」になったからです。
公式ドキュメントも、書かれた時点の情報にすぎません。

---

## 8. 新しい教材を作るときのチェックリスト

```
□ 正しさの担保を決めた（何を、どうやって機械検証するか）
□ いちばん怪しい部分の実現可能性を、着手前に確かめた
□ 本文のコードは実ファイルを読み込んでいる（書き写していない）
□ 掲載コードと実ファイルの一致を、機械で照合している
□ サンプルは全部、単体で実行できる（教材専用のヘルパを import していない）
□ サンプルは全部、自動で実行検証されている
□ 「〜になる」という挙動の主張は、実物で確かめてから書いた
□ 対応バージョン・依存・ライセンスは一次情報から取った
□ 素材（画像・出力）は毎回同じ結果になる（状態を持ち込んでいない）
□ 生成物を 1 つずつ目で見た（自動検証は「壊れていない」しか言わない）
□ 明暗テーマ・スマホ幅で崩れないか確認した
□ 症状から引ける索引がある
□ 環境差（見た目・バージョン）を本文で明言した
□ 検証環境のバージョンをページに表示している（手書きせず実測値を出す）
```

---

## 9. このリポジトリ固有の話

- 各教材は `<教材名>/tools/build.py` で `docs/<教材名>/` を生成します
- 教材をまたぐもの（一覧ページ、旧 URL からの転送）は `tools/build_hub.py` が担当します
- 章立ての情報源は `<教材名>/book/toc.py` だけです。ナビも目次も前後リンクもそこから生成されます
- 公開前に走らせるものの一覧は、リポジトリ直下の `README.md` にあります

**教材ごとに、担保のしかたは変わって構いません。**
Qt6 の教材はスクリーンショットで担保しましたが、
Rust の教材は端末出力の捕獲と、コンパイルエラーのエラーコード検証で担保しています。
共通しているのは「**本文の主張を、実物に対して機械で確かめている**」という一点だけです。
そこさえ守れば、方法は分野に合わせて変えてください。
